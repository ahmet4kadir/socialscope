# Dependencies
node_modules/

# Local data: SQLite database (+ WAL/SHM sidecars)
data/

# Scraper debug dumps (screenshots + HTML on selector failure)
debug/

# Logged-in browser sessions (cookies) — never commit these
.sessions/

# Next.js
.next/
out/

# Environment / secrets
.env
.env.*
!.env.example

# Build output & caches
dist/
*.tsbuildinfo

# OS noise
.DS_Store
Thumbs.db

# Claude Code personal settings (launch.json is shared, this is not)
.claude/settings.local.json
